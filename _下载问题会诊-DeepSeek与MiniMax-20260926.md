# 下载问题会诊（DeepSeek + MiniMax）—— 逐条对源码核实后

> 2026-09-26 晚。方法：把我的分析原文（含 4 个文件的代码事实）发给两家审，回收后**每条都翻源码核对**。
> 过程记录：首轮 DeepSeek ✔、豆包**返回提示词回显（无答案）**→ 按协议改派 → MiniMax 补上 ✔。

## 一、能当场定案的（这条我原来只敢叫"假设"）

**ffmpeg 是「进程内」跑的，不是子进程** —— `Hook.m` 头部注释 + `FFmpegConverter.swift:41-52`：
用 `setjmp/longjmp` 兜住 ffmpeg 的 `exit()`，直接调 `FFmpeg_main`（符号来自 fftools 静态库）。

→ **结论：机制 ⑥ 成立（不是"可能"）** —— 大文件转码时 ffmpeg 吃的内存**就是我们的内存**，
必然更容易撞系统内存上限。两家在这一条上**结论相反**（DeepSeek："计入同一限额" / MiniMax：
"posix_spawn 子进程独立限额"）—— **MiniMax 的前提就不存在：这里根本没有子进程。**

★ 另一条同类的：`TSRemuxer` 的背压**是真的**（`:856-865` 等 `isReadyForMoreMediaData`），
2ms 只是轮询间隔。**MiniMax 说"2ms sleep 不是真背压、队列无界增长"—— 不成立。**

## 二、两家一致同意的（采纳）

| 结论 | 来源 | 我的处理 |
|---|---|---|
| ①②④⑤ 成立；④（不支持 Range 时整份进内存）**最致命** | 两家 | 采纳，列为第一优先修 |
| **P2-2 直链多连接 Range 并发——不推荐** | 两家都反对（被判盗链 403/限速） | **撤回我的提案** |
| HLS 并发 **6** 是稳的上限（8 会触发部分 CDN 挑战页） | 两家 | 采纳 6，不做 8 |
| `URLSession.download(for:)` 落盘**不堆内存**，但 temp URL 在回调返回后即删，**必须同步 move**；且要用 **ephemeral** 配置（否则 cache/credential 仍在进程内） | 两家 | 采纳（含 ephemeral） |
| 只比"`.ts` 大小 == Σ分片"**不够** → 要**显式完成标记**（DeepSeek 说"半文件也可能凑巧相等"，MiniMax 提 manifest + `.complete` 哨兵） | 两家 | 采纳：写**完成标记文件**（记录分片数与总字节），下次据此判定"已拼接"，而不是靠猜 |
| **确定性失败不要自动重试**（磁盘满 / 类型不支持），否则是二次伤害 | MiniMax | 采纳 |
| 磁盘满/写入失败的错误码不统一（`NSPOSIXErrorDomain 28`、`NSFileWriteOutOfSpaceError`、ffmpeg 退出码各报各的）→ 要**识别并给人话** | 两家 | 采纳（转成"空间不够，还差 X MB"） |
| 转码前**预检可用空间** | 我的 P0-3，两家认可 | 采纳 |

## 三、被推翻 / 不采纳的

1. **我的 P2-2（直链多连接并发）** —— 两家一致反对，撤回。
2. **MiniMax「TSRemuxer 没有真背压」** —— 源码里明明在等 writer 就绪，推翻。
3. **MiniMax「转码后要校验 .mp4 的 moov」** —— 半对：`FFmpegConverter.swift:61-67` **已经**校验
   "输出里有视频轨"（没轨就抛错），且参数带 `-movflags +faststart`（moov 直接写在文件头）。
   再独立查 moov 的价值有限 → **本轮不做**，记进候选。
4. **DeepSeek「申请 increased-memory-limit entitlement 提内存上限」** —— 真实存在的 entitlement，
   但**是否对巨魔侧载生效我没有依据**（Apple 正常要审），标为**待真机验证的候选**，不在本轮做。

## 四、留给真机验的（不写进代码，只写进清单）

- 「零声音纯激活音频会话会不会掐掉音乐」（上一轮的遗留）
- 「加了 increased-memory-limit 是否真的放宽上限」（可选实验）
- 卡死/闪退仍是三条路并存（主线程 / 内存 / 引擎慢），**判据仍是那条任务的过程记录**

## 五、合并后本轮做什么（v1.0.101）

1. **直链流式落盘**（ephemeral + `download(for:)` 立即 move）→ 消灭"整份进内存"这个必崩点
2. **临时文件清理入口** + **分片占用计入统计**（现在完全看不到）
3. **断点可信化**：写完成标记（分片数 + 总字节）→ 校验通过**即清分片**（2× 只存在拼接那十几秒）；
   完成标记在且 `.ts` 大小一致 → 重试**直接跳到转码**
4. **转码前预检空间** + 空间不足/类型不支持**不自动重试**、给明确人话
5. **HLS 并发 4 → 6**

> 不做：直链多连接并发（两家一致反对）。
