════════════════════════════════════════════════════════════
  视频抓取（VideoGrab）  v1.0.4   安装说明
════════════════════════════════════════════════════════════

文件    dist\VideoGrab-unsigned.ipa   （253,810 字节）
仓库    https://github.com/luowenbin061-blip/video-grab
构建    2026-09-23  GitHub Actions / macos-14 / Xcode 15.4
适用    iOS 15.0 以上（你的 16.6 没问题）

★ v1.0.4 新增：下载完自动转 MP4
  上一版下出来的是 .ts，系统播放器和微信都不认。
  这一版下载完自动转成 .mp4（只换封装、不重新编码，
  47 分钟的视频十几秒就完，画质无损）。
  另外加了「在 App 里播放」，下载完点一下直接看。

装过 v1.0.3 的话：建议先在桌面长按删掉旧版，再装这个。

────────────────────────────────────────────────────────────
这个 App 是干什么的
────────────────────────────────────────────────────────────

它自带一个浏览器。网页视频的真实地址只存在于「加载它的那个
会话」里 —— 所以要在自己的浏览器里打开、在自己进程里嗅，
才拿得到。嗅到之后下载、拼接、转 MP4，最后落到「文件」App 里。

不需要付费，不需要解锁，没有任何内购。

────────────────────────────────────────────────────────────
怎么装
────────────────────────────────────────────────────────────

1. 把 dist\VideoGrab-unsigned.ipa 传到手机
   （微信「文件传输助手」发给自己 → 存到「文件」App）

2. 用 TrollStore 打开这个 IPA 安装
   （自签工具如轻松签也可以）

3. 装完打开一次。

没有扩展要开、没有描述文件要信任 —— 它就是个普通 App。

────────────────────────────────────────────────────────────
怎么用
────────────────────────────────────────────────────────────

1. 地址栏输入视频站地址，进去

2. ★ 点一下播放，让视频真的开始加载（至少播几秒）
   —— 这一步不能省。地址是播放时才产生的。

3. 长按视频画面，或点右下角那个圆圈
   → 弹出嗅探结果面板

4. 选标着 M3U8 的那一条 → 点「开始下载」
   （标 TS 的是分片，别选那个；标 MP4 的是直链，也可以）

5. 下完：
   · 转 MP4 成功 → 点「在 App 里播放」直接看，
     或者去「文件」App → 我的 iPhone → 视频抓取 里拿文件
   · 转 MP4 失败 → 会保留 .ts，失败原因写在下载条目里

────────────────────────────────────────────────────────────
下载时注意
────────────────────────────────────────────────────────────

尽量别切出去。iOS 会在 App 切到后台后把它挂起，
下载会暂停。已下好的分片会保留 —— 回来再点一次会接着下，
不用从头来。

────────────────────────────────────────────────────────────
已知限制
────────────────────────────────────────────────────────────

· 转 MP4 这一步，前提是 iOS 能读出这个 .ts 里的视频轨。
  理论上可以（iOS 播 HLS 本身就是在读 MPEG-TS），但本地文件
  能不能读，只有实测知道。如果转失败，界面上会明确写：
  「系统读不出这个 .ts 里的视频轨」—— 那说明 iOS 不认这个
  封装，只能用 VLC / nPlayer 看 .ts。把这句话告我。

· DRM 加密的付费影片拿不到，任何工具都做不到。

· 有些站的地址是脚本算出来的、或走了第三方解析接口，
  可能嗅不到 —— 换个线路、或让视频多播一会儿再刷一次。

· 个别站会检测「是不是 App 内置浏览器」，那种站打不开也正常。

· 这次是第一次跑，嗅探和下载逻辑都没在真机上验证过。
  如果某个站嗅不到，或者下载卡住，把现象告我 ——
  面板里的「来源」那一行能看到地址是从哪抓到的，很有用。

────────────────────────────────────────────────────────────
这次构建验证过什么
────────────────────────────────────────────────────────────

✅ 编译通过（xcodebuild exit 0）
✅ 主二进制有 LC_CODE_SIGNATURE（已 ad-hoc 签名）
✅ 有 _CodeSignature/CodeResources
✅ sniffer.js 已打进 App 包
✅ Info.plist 三项关键配置：
     UIFileSharingEnabled              = True
     LSSupportsOpeningDocumentsInPlace = True
     NSAllowsArbitraryLoads            = True

❌ 没验证过的：转 MP4 在真机上能不能跑通
   —— 本地没有 iOS 设备，这一步只能靠你实测。
   如果失败，把界面上那句错误原文告我，能直接定位问题。

────────────────────────────────────────────────────────────
上一版（v1.0.3）已经实测确认的
────────────────────────────────────────────────────────────

✅ 打开网站、嗅探到 m3u8 地址 —— 通了
✅ 下载 694 个分片、拼接成文件 —— 通了
✅ 文件落到手机上 —— 通了
（用户实测反馈："成功下载"）
