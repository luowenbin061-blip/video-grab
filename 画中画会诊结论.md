# 画中画起不来 —— 多方会诊结论与处理

> 问题：iOS 16.6（TrollStore 侧载）上做 sample-buffer 画中画保活，始终报
> `AVKitErrorDomain -1001: The UIScene for the content source has an activation state
> other than UISceneActivationStateForegroundActive, which is not allowed.`
> 同 App Store 上的 Stay 用同样机制却能完美运行。
> 处理：把完整代码与报错演进史一起交给多方独立分析，收回结论收敛后落到代码（v1.0.36）。

---

## 一、结论一：病灶是「承载画面的视图没真正进到前台活跃的窗口里」

两次独立分析给出几乎同样的判断，原文要点：

> 真正的触发点不是「scene 类型」，而是**创建 ContentSource / 调用 startPictureInPicture
> 的那一刻，承载 `AVSampleBufferDisplayLayer` 的视图尚未挂到一个 foregroundActive 的
> 真实 UIWindow 上**。
>
> 你用 `UIViewRepresentable` 的 `.background` 挂一个 3×3 的视图，`makeUIView` 返回时
> `view.window == nil`；content source 关联到一个无窗口的层，AVKit 记下的场景关联
> 从一开始就不合法 —— **懒创建或重建都没用**，因为重建时机同样早于 `didMoveToWindow`；
> 而且 3×3 不满足「inline 视图非零」的隐含要求。

**处理**：
- 不再让 SwiftUI 的 `.background` 承载那个视图，改由 App 自己把宿主视图
  （**180×101**，明确非零）`insertSubview(at: 0)` 加到 **keyWindow 最底层**
  （被上层不透明页面遮住，用户看不见）；
- 移除 SwiftUI 侧的 `PiPHost`（避免两个父视图抢同一个 layer）；
- **建控制器之前**先确认 `layer.window != nil`，不满足就等，不浪费尝试次数。

---

## 二、结论二：送帧必须走固定节奏，且与「启动重试」各走各的

> 你的重试梯子间隔最长到 2 秒，**等于在饿着层**。画布层长时间收不到帧会自己进
> `failed`（就是 -11847）；**层一旦 failed，后面重试多少次 `startPictureInPicture()`
> 都不会成功** —— 这正是「重试 10 次全失败」的一个很可能的原因。
> 正确做法：`renderFrame()` 走**固定定时器**，`startPictureInPicture()` **单独**走梯子。

**处理**：
- 送帧改成 **0.35 秒一帧**的固定节奏；
- 在**点开关那一刻**就开始送帧（不再等小窗起来才开）；
- 挂在 `RunLoop.main` 的 `.common` 模式（滚动页面时也不断）；
- 三条「放弃」分支都会收掉定时器，不空转耗电。

---

## 三、结论三：不换实现路线

- 视频通话那套（`AVPictureInPictureVideoCallViewController` + `activeVideoCallSourceView`）
  **不能绕过这个错误** —— 两条路都要求 `start` 那一刻场景前台活跃，-1001 是**场景绑定规则**，
  不是 `AVSampleBufferDisplayLayer` 特有的。
- 而且视频通话路径的小窗内容是**对源视图的栅格捕获**，刷新控制弱、无法自定帧时序。
- **Stay 走的正是我们现在这条路**（sample-buffer），因为它需要完全控制帧的时序与分辨率。
  → 继续走 sample-buffer，不折腾。

---

## 四、附带收获：iOS 后台保活手段的真实有效性排序

| 手段 | 可行性 | 预期后台时长 | 关键限制 |
|---|---|---|---|
| sample-buffer 画中画 | 侧载可用（App Store 禁止） | 无严格时限，锁屏可续 | 需前台触发合法场景；层必须持续有帧 |
| 静音音频 + `UIBackgroundModes=audio` | 通用 | 3–10 分钟 | 纯下载无音频渲染易被判闲置查杀 |
| 定位后台模式 | 侧载可用（审核禁止） | 长期 | 耗电极高，低电量直接失效 |
| `BGProcessingTask` / `BGAppRefreshTask` | 通用 | 数十秒且不保证唤醒 | 配额极严、优先级低 |
| `beginBackgroundTask` | 通用 | **硬性 30 秒**，不可续期 | 只够瞬时收尾 |
| `background URLSession` | 通用 | 较长 | **只托管网络请求**，下载后解密/拼接/转码的业务逻辑无法执行 → **本项目不适用** |
| TrollStore 侧载特权 | — | — | 能绕审核与部分校验，**不能绕 UIScene / AVKit 底层校验** |

**架构结论**：以 sample-buffer 画中画为核心，不指望后台任务与后台 URLSession；
唯一能绕开 PiP 的备选是定位保活，代价是耗电。

---

## 五、落地清单（v1.0.36 已实现）

1. 层挂到 keyWindow 最底层，尺寸 180×101，不依赖 SwiftUI 承载
2. 建控制器前确认层已在窗口里（窗口闸门）
3. 送帧 0.35s 固定节奏，独立于启动重试，RunLoop `.common`
4. 第一级启动延后 0.6 秒（不贴着确认框关闭那一瞬间调）
5. 每级尝试前确认场景处于前台活跃，不在就等（**不消耗尝试次数**）
6. 重试 12 级（约 10 秒），第 4 级整个重建一次控制器与 content source
7. 放弃时报错带全：层状态 / 场景状态 / 音频会话 / 系统原话
8. 音频会话 `playback` + 已激活（前置条件，`UIBackgroundModes=audio` 已声明）

**若仍失败**：把界面上那行完整报错发回 —— 它会明确指向是层没进窗口、场景不活跃，
还是系统给的别的原因。
