# 书签两级拖动排序 —— 五家 AI 会诊结论（2026-09-26）

> 起因：v1.0.97/98 加的「排序模式」真机实测**彻底失败**（用户：很多 bug、各种各样的问题）。
> 做法：把问题拆成 5 个独立角度（根因 / 数据模型 / SwiftUI 写法 / 落点语义 / 替代方案），
> 并行分发；回收后逐条对**真实源码**核实（AI 说「这里没做」≠ 真的没做）。
> 结果：**5 份都是真答案**（其中一份头两次只回显了提示词，已改派重发）。

---

## 一、一句话结论

**「把分组行和书签行拍平到同一层、用一张 onMove 管两级」这个设计是错的 —— 两家独立答案都指向同一个替代方案：把两级顺序拆到两层界面里去管。而且它还有两个独立的硬 bug（分开 publish + 在手势回调里直接写数据源），就算设计对了也一定会抖。**

---

## 二、我原先怀疑的 5 条 → 裁定

| 我的怀疑 | 裁定 | 依据 |
|---|---|---|
| ① `sortRows` 是计算属性 → 拖拽中数据源一直在重算 | **成立** | 回调里直接 `store.applyFlat(...)`，立刻触发重渲染，与拖拽识别器抢状态 |
| ② 顺序有两处真值（`groupOrder` + marks 里 folder 首现序） | **成立** | `applyFlat` 里 `marks.sort{}` 与 `groupOrder = order` 是**两次独立赋值** → 两次发布 → 中间帧两条真值不一致 → **这就是「分组跳回原位 / 跑到末尾」的直接来源** |
| ③ 「我的收藏」恒渲染在最后，但保存语义允许它居中 | **成立** | 显示与落点解释矛盾 → 拖了弹回 |
| ④ 自绘手柄 + 系统手柄重复 | **成立**（不影响数据，只糊） | 代码里确实自绘了 `line.3.horizontal`，编辑模式下系统也会画 |
| ⑤ 拍平后落点语义跟直觉不一致 | **成立** | 见第三节：业界都是「分组行 = 容器边界」，我给的是「就近归属」，两者不是一回事 |

### 还挖出 3 条我没想到的（按致命度排）

1. ★★ **`.environment(\.editMode, .constant(.active))` 会和 sheet 的下滑关闭手势打架、卡在编辑态退不出来。**
   我的排序列表正好在 `.sheet` 里 —— 这**极可能就是「各种各样的问题」的主要来源**。
   正确姿势：`@State var editMode: EditMode = .inactive` + `.environment(\.editMode, $editMode)`，
   或者直接用系统 `EditButton()`。
2. ★ **`.onMove` 回调里不准直接改数据源**。应先把行缓存到本地 `@State`，拖动过程只改本地缓冲，
   **手势结束后一次性提交**给 store（再落盘）。
3. ★ **Drag 落点必须配视觉引导**（像 Notion 那种蓝线），否则用户分不清「归入组内」和「插入组间」。

### 被推翻的一条（重要：AI 也会看错）

- 有一条说「`Bookmark` 没有 id 字段 → 行标识不稳 → 一切失效」。
  **核实：`Bookmark` 有 `var id: String { url }`，`SortRow` 的 id 也是稳定构造（g:/m: + key）→ 这条不成立。**
  （它背后的原则「拖拽期间行标识必须恒定」是对的，但我的代码这点没问题。）

---

## 三、落点语义该是什么样（业界怎么做的）

| 情形 | 直觉解法 | 先例 |
|---|---|---|
| 书签拖到**两个分组行之间** | **归入上方那个分组**（分组行 = 容器边界，落在它上方就属于它） | Safari 书签（拖到文件夹上方归入该文件夹） |
| **分组行**拖到另一组的书签中间 | **整体插入为同级分组**，**不嵌套**进目标分组 | 「提醒事项」拖列表到列表之间 = 调整顺序；拖到某个列表**之上**才是嵌套 |
| 视觉 | 必须画**引导线**区分「归入组内」vs「插入组间」 | Notion 的蓝色横线 |

补充（老实说）：**备忘录只支持同级重排、不支持嵌套；文件 App 是「拖到文件夹上=移入，拖到之间=同级排序」；
Notion / Things 的具体行为回答方标注了「不确定」—— 这部分没编，可信度自己留了口子。**

> 注意：我们的分组名是**扁平字符串**（「书签栏 / AI」），本来就**不支持嵌套** ——
> 所以"分组行不许嵌套"这条不是妥协，而是**跟数据结构一致的**。

---

## 四、五家给出的替代方案（融合）

### ★ 推荐（两家独立命中）：把两级顺序拆到两层界面

1. **一层只管分组顺序**：列出所有分组，用 ↑/↓ 或原生 `onMove` 排；
2. **点进某个分组 → 只管这一组内的顺序**：同样是单层列表 + `onMove`；
3. **跨组移动**：沿用**已经做好**的「左滑 → 移到某个分组」（v1.0.95 就有了）；
4. 一层列表里**永远只有一种东西**（要么全是分组行，要么全是书签行）→ 没有落点歧义、没有混层规范化的怪问题。

配套的写法要求：
- `EditMode` 用 `@State` + `EditButton()`，**不要** `.constant(.active)`（会和 sheet 手势打架）；
- `onMove` 回调里先改**本地 `@State` 缓冲**，手势结束再一次性 `store.apply(...)`；
- 一次提交里只发**一次** `objectWillChange`（把 marks 与 groupOrder 的改动合并成一次）；
- 不要自绘手柄，用系统的。

### 另一个方案（一家推荐）：干脆不要拖

- 顶部一个「整理」按钮 → 独立整理层，行内用显式 **↑ / ↓** 按钮；点进某组再排组内；
- 代价：放弃"直接操纵"的流畅感、远距移动要按多次；
- 理由：**几百条书签这个规模下，"拖"本身又慢又容易误触，排错成本超过收益。**

> 两家的取舍不同，但**指向同一个动作**：把顺序编辑从主列表里拿出来，放进一个专门的层。
> 区别只是那一层里用「拖」还是用「↑↓」。

### 可选的数据模型升级（一家建议）

把分组**实体化**：`struct Group { id: UUID; name: String }` + `Bookmark.groupID: UUID?`，
`groups` 数组的顺序 = 组间顺序，`marks` 的相对顺序 = 组内顺序 → **任何时刻只有一处真值**；
重命名分组不碰书签；空分组天然支持。
**代价**：要写一次数据迁移（把现有的 `folder` 字符串映射成 groupID）。这是"根上干净"的做法，
但**不是这次必须做的** —— 先修交互，模型可以下一版再升级。

---

## 五、我打算怎么做（等用户拍板）

| 步骤 | 内容 |
|---|---|
| **1. 立刻回退** | 把「排序」按钮 + 拍平列表**整个拿掉**（当没做过）；**回收站保留**（它没出问题） |
| **2. 重做（推荐路线）** | 「整理」独立层：一层排分组顺序（↑↓ 或 onMove）→ 点进去排组内顺序；跨组移动沿用现有左滑 |
| **3. 顺手修两个硬 bug** | ① 一次提交只发一次 `objectWillChange`；② `onMove` 不再直接写 store |
| **4. 可选** | 数据模型实体化（分组变实体）——根上解决"两处真值"，但要写迁移 |

**不推荐继续在原设计上加补丁**：拍平 + 就近归属这套，落点语义本身就是跟直觉相反的，
补丁只能让它"不那么容易崩"，不会让它变得好用。

---

## 附：这次会诊的过程记录（可复现）

- 5 道独立子问题 → 分别派发；**4 家一次成功，1 家失败 1 次**（`ALL_EXHAUSTED`：
  5 家全设成 primary 导致每个 worker 没有 fallback，已按协议改派补发）。
- **1 家首次返回的是「提示词回显」而不是答案** —— 校验器因为锚行在回显里而误判 PASS，
  靠人工核对才发现，改派后拿到真答案。**教训：机器校验只能拦格式，拦不住"答非所问"。**
